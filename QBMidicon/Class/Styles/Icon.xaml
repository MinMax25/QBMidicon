<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ICON Style -->
    <Style x:Key="BasePackIconStyle" TargetType="iconPacks:PackIconBase">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>

    <!-- ICON Resources -->
    <iconPacks:PackIconMaterial x:Key="SourceDataPageIcon" Kind="FileMusicOutline" Style="{StaticResource BasePackIconStyle}"/>
    <iconPacks:PackIconMaterial x:Key="ConvertedDataPageIcon" Kind="ArchiveArrowDownOutline" Style="{StaticResource BasePackIconStyle}"/>
    <iconPacks:PackIconMaterial x:Key="EventFilterPageIcon" Kind="FilterOutline" Style="{StaticResource BasePackIconStyle}"/>
    <iconPacks:PackIconMaterial x:Key="ConvertSettingPageIcon" Kind="FileSettingsOutline" Style="{StaticResource BasePackIconStyle}"/>

    <iconPacks:PackIconMaterial x:Key="SettingPageIcon" Kind="CogOutline" Style="{StaticResource BasePackIconStyle}"/>

</ResourceDictionary>
